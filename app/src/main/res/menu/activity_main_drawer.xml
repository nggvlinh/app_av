<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/home"
            android:icon="@drawable/home"
            android:title="Home" />
    </group>

    <item android:title="TEST">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/vo"
                    android:icon="@drawable/ic_menu_send"
                    android:title="Vocabulary" />
                <item
                    android:id="@+id/luyentv"
                    android:icon="@drawable/ic_menu_send"
                    android:title="Practice Vocabulary" />
                <item
                    android:id="@+id/gra"
                    android:icon="@drawable/ic_menu_send"
                    android:title="Grammar" />
                <item
                    android:id="@+id/search"
                    android:icon="@drawable/ic_menu_send"
                    android:title="Search Question" />
                <item
                    android:id="@+id/score"
                    android:icon="@drawable/ic_menu_send"
                    android:title="List of point" />
            </group>
        </menu>
    </item>

</menu>
